/*Write a program to calculate sum of cube of first N natural numbers*/

#include <stdio.h>
 int main() {
 int count=0,num;
 printf("Enter a Number");
 scanf("%d",num);
 do{
	num /=10;
    ++count;

 }while(num != 0);

 printf("%d",count);
  return 0;
}