/*Write a program to check whether a given number is a Prime number or not*/


#include <stdio.h>
 int main() {
 int i,n,count=0;
 printf("Enter a Number");
 scanf("%d",num);
 for(i=2;  i <= n-1; i++)
 {
 	if(n%i==0){    	
        count += 1;
        break;
    }
     
 }
 if(count > 0)
 {
   printf("%d is Not a  Prime Number. it is divisible by %d.",n,i);
 }else
 {
  printf("%d is a  Prime Number.",n);
 }
  return 0;
}